# =================================================================
# Environment Variables Template
# =================================================================
# Copy this file to .env.local and fill in your actual values
#
# NEVER commit .env.local to git (it's in .gitignore)
# =================================================================

# Database Configuration
# Get this from Supabase: Settings → Database → Connection Pooling
DATABASE_URL="postgresql://postgres:[YOUR_PASSWORD]@db.[YOUR_PROJECT_REF].supabase.co:6543/postgres?pgbouncer=true"

# Supabase Configuration (for when you add it)
NEXT_PUBLIC_SUPABASE_URL="https://[YOUR_PROJECT_REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"

# Discord OAuth (from Discord Developer Portal)
DISCORD_CLIENT_ID="your_discord_client_id"
DISCORD_CLIENT_SECRET="your_discord_client_secret"

# NextAuth Configuration
# Generate with: openssl rand -base64 32
AUTH_SECRET="your_generated_secret_here"

# App URLs
# Development
AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Production (uncomment and update when deploying)
# AUTH_URL="https://your-domain.vercel.app"
# NEXT_PUBLIC_APP_URL="https://your-domain.vercel.app"

# =================================================================
# EA API Proxy Configuration (IMPORTANT FOR PRODUCTION)
# =================================================================
# EA blocks Vercel's serverless function IPs with 403 Forbidden.
# To fix this, deploy a Cloudflare Worker as a proxy.
#
# See CLOUDFLARE_SETUP.md for step-by-step instructions.
#
# After deploying your worker, paste its URL here:
EA_PROXY_URL="https://ea-proxy.YOUR-SUBDOMAIN.workers.dev"
#
# If not set, the app will fall back to direct fetch (works locally,
# but will get 403 on Vercel in production).
